{
  "name": "clerk-auth-app",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "app",
    "api"
  ],
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "start": "pnpm -r start",
    "lint": "pnpm -r lint"
  }
}
